#include "Input.h"

// Awais: add the SFML header when you start implementing this function.

void handleInput(sf::RenderWindow& window, Chip8& chip8)
{
    // TODO: handle SFML events, including closing the window.
    // TODO: map physical keys to chip8.key[0] through chip8.key[15].
    // Set a key to 1 on press and 0 on release.
    // Clear held keys when the window loses focus.
}
