#include "Display.h"

// Awais: add the SFML header when you start implementing these functions.

void setupGraphics(sf::RenderWindow& window)
{
    // TODO: create the SFML window for a scaled-up 64 x 32 display.
}

void drawGraphics(sf::RenderWindow& window, const Chip8& chip8)
{
    // TODO: read chip8.gfx[y * 64 + x] and draw the pixels with SFML.
    // Main clears chip8.drawFlag after drawing.
}
